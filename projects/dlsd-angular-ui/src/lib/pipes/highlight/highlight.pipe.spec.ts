import { DLSDHighlightPipe } from './highlight.pipe';

describe('HighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new DLSDHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
