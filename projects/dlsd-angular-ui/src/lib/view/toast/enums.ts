export enum DLSDToastColor {
  RED = 'red',
  GREEN = 'green',
  GRAY = 'gray',
  YELLOW = 'yellow',
}

export enum DLSDToastType {
  INVALID_DATA = 'invalidData',
  SERVER_ERROR = 'serverError',
  SAVED_CHANGES = 'savedChanges',
}
